import { MusiquefullPipe } from './musiquefull.pipe';

describe('MusiquefullPipe', () => {
  it('create an instance', () => {
    const pipe = new MusiquefullPipe();
    expect(pipe).toBeTruthy();
  });
});
