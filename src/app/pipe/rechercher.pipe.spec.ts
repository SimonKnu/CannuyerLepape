import { RechercherPipe } from './rechercher.pipe';

describe('RechercherPipe', () => {
  it('create an instance', () => {
    const pipe = new RechercherPipe();
    expect(pipe).toBeTruthy();
  });
});
